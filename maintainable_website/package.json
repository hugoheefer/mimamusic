{
  "name": "mimamusic-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MiMaMusic — static website (Eleventy) with Pages CMS editing.",
  "license": "UNLICENSED",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "node -e \"import('node:fs').then(fs => fs.rmSync('_site', { recursive: true, force: true }))\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^7.0.0",
    "markdown-it": "^14.1.0"
  }
}
